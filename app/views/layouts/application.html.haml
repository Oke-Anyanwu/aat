!!!
%html{:lang => "en"}
  %head
    %meta{:charset => "utf-8"}/
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}/
    %title
      = yield(:title)
    = stylesheet_link_tag    "application"
    = javascript_include_tag "application"
    = javascript_include_tag "vendor/modernizr"
    = csrf_meta_tags
  %body
    = render 'layouts/top_nav_bar'
    #main-content.row
      .columns
        #flash-messages
          #not-authorized-flash.alert-box.alert.radius{style: 'display:none;',data: {alert: ''}}
            You are not authorized to perform this action
          - unless flash.empty?
            - flash.each do |key, value|
              .alert-box.radius{class: flash_class(key), data: {alert: ''}}
                = value
                %a.close{href: '#'} &times;

        = yield
    = yield :page_js
