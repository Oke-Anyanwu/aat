= content_for :title, "Leaves"

.row
  .large-12.columns
    %h3 All leave requests

    %table.table
      %thead
        %tr
          %th Employee
          %th Leave Date
          %th Reason
          %th Options
      %tbody
        = render @leaves
