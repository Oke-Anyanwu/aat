= content_for :title, "Employees"

.row
  .large-12.columns
    %h3 Employees

    %table.table
      %thead
        %tr
          %th Last Name
          %th First Name
          %th Email
      %tbody
        = render @employees
