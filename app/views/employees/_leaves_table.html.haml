%table.table#leaves-table
  %thead
    %tr
      %th Date
      %th Reason
  %tbody
    - leaves = @employee.leaves
    - if leaves.length > 0
      = render partial: "leave", collection: @employee.leaves
